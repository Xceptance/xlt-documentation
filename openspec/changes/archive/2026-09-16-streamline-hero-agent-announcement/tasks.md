## 1. Hero Banner Top Announcement Pill & Bottom Pill Removal

- [ ] 1.1 Replace the static hero category badge in `pages/index.astro` with an interactive announcement pill `Connect your Agent via /mcp (Antigravity, Claude Code...) →` linking to `#mcp-server-section`.
- [ ] 1.2 Remove the redundant bottom MCP pill located underneath the suggested search pathway chips in `pages/index.astro`.

## 2. Verification & Validation

- [ ] 2.1 Run `npx blume validate` and verify zero broken links, anchor errors, or configuration issues.
- [ ] 2.2 Run `npx blume build --isolated` and verify successful production build generation without warnings or errors.

